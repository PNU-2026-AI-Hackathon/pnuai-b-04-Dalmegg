# Unity WebGL Build

Place the Unity WebGL build output in this directory.

Expected runtime entry:

```text
web/public/unity-simulator/index.html
web/public/unity-simulator/Build/
web/public/unity-simulator/TemplateData/
```

After the files are added, the web simulator page loads `/unity-simulator/index.html`.
